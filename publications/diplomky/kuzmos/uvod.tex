\chapter{Úvod}

\section{Úvod do dataminingu}

\subsection{Pøehled}
\paragraph{}
Datamining (DM), èesky také jako Dobývání znalostí z dat,
rovnìž známý jako KDD (Knowledge-Discovery in Databases)
je strojové ziskávání netriviálních, døíve neznámých
skrytých vzorcù z dat získaných pozorováním nebo velkých databází.
Takto nalezené vzorce mohou být reprezentovány ve srozumitelné
podobì s cílem dalšího jejich použití. Zde se budeme zabývat
nalezením vzorcù v podobì asociaèních pravidel (podrobnìji viz
\ref{asociacnipravidla})
\paragraph{}
Datamining stojí na pomezí vypoèetních
statistických metod, databázových technologii a strojového uèení.
Metodami dobývání znalostí se dají øešit úlohy deskripce dat, sumarizace,
segmentace, deskripce konceptù, klasifikace, predikce a analýzy závislostí
v celé øadì aplikaèních oblastí.

\subsection{Relaèní datamining}
\paragraph{}
Existující metody dobývání znalostí byly pøevážnì navrženy pro data uložena
v jedné datové matici (tzv. flat-file). Pøi takové organizaci dat øádky datové
matice reprezentují pozorování (objekty studia, osoby, záznamy, pøedmìty apod.)
a sloupce datové matice reprezentují promìnné (vlastnosti, atributy nebo
charakteristky). Aèkoliv takový pohled na zkoumaná data zjednoduší metody
používané pro dobývání znalostí, v praxi se taková jednoduchá reprezentace dat
nevyskytuje èasto. Je sice možné data dostat do této podoby, avšak takový úkon
je èasovì velmi nároèný a èasto se musí provést manuálnì.
\paragraph{}
Nìkteré úlohy pro dobývání znalostí navíc nejdou jednoduše provést nad "plochou"
reprezentaci, obzvláštì v pøípadì, kde uvažujeme mnoho rùzných objektù s jejich
vlastnostmi a vzájemnými vztahy. Cílem relaèního dataminingu je odstranìní
omezení na jednu tabulku dat a umožnìní dobývání znalostí z více relaènì
propojených datových tabulek.
\section{GUHA}
\paragraph{}
GUHA je zkratka pro General Unary Hypotheses Automaton.
Tuto metodu poprvé formuloval Hájek a kol. již v roce 1966,
detailnìji pak Hájek a Havránek (1978).
\paragraph{}
GUHA je druh automatizované explorativní analýzy dat.
Základní princip GUHA metody je v generovaní všech možných relevantních otázek
specifikovaných uživatelem, ovìøení každé z nich oproti datùm a reportování
pouze prostých hypotéz. Hypotéza je prostá, jestliže je pravdivá 
v analyzované datové matici a zároveò nevyplývá z jiných, jednodušších hypotéz. 
\subsection{Metody DM}
\paragraph{}
Pøi tvorbì dataminingového modelu lze použít více metod. Dobrých výsledkù
dosahují modely kombinující více metod. Jako pøíklad mùžeme uvést statistické
procedury, rozhodovacé stromy, neuronové sítì nebo asociaèní pravidla, se
kterými pracuje metoda GUHA a kterými se zde budeme podrobnìji zabývat. Pro
podrobnìjší pøehled metod pro tvorbu dataminingového modelu viz napøíklad 
\cite{duchacek}.
\subsection{Asociaèní pravidla}
\label{asociacnipravidla}
\subsubsection{Apriori}
\paragraph{}
Asociaèní pravidla mùžeme rozdìlit na dvì skupiny. První se vztahuje k analýze
nákupního koše. Takové pravidlo je ve tvaru $X \to Y$, kde $X$ a $Y$ znaèí
dvì množiny pøedmìtù, které se mohou nacházet v nakupních koších a má
následující význam: když nákupní koš obsahuje množinu pøedmìtù $X$, 
pravdìpodobnì bude obsahovat množinu pøedmìtù $Y$. Napøíklad klienti, kteøí
nakoupí víno, èasto nakupují také sýry. Analýzou transakcí supermarkety mohou
optimalizovat rozmístìní zboží v prodejnì a zajistit rychlejší a výnosnìjší
nakupování. Pro dobývání znalostí za využití takových pravidel byl navržen
algoritmus Apriori, viz \cite{apriori}.
\subsubsection{GUHA}
\paragraph{}
Druhou skupinou asociaèních pravidel popisuje metoda GUHA. Je obecnìjší než
Apriori. Tato metoda byla poprvé formulována v roce 1966 a byla vydana v 1978
 - viz \cite{guhabasic}. Asociaèní pravidlo má tvar $\phi \approx \psi$, pøípadnì
 $\phi \approx \psi / \chi$ pro podmínìná asociaèní pravidla,
kde $\phi$, $\psi$ a $\chi$ znaèí konjunkce boolských atributù odvozené
z hodnot sloupcù
analyzované matice dat. Symbol $\approx$ znaèí obecný kvantifikátor, jenž
popisuje vztah (asociaci) mezi $\phi$ a $\psi$. Tento mùže být napøíklad
ve formì implikace nebo ve formì statistického testu. Asociaèní pravidlo
$\phi \approx \psi$ tedy znamená, že $\phi$ a $\psi$ spolu souvisí ve smyslu
použitého kvantifikatoru $\approx$, pøípadnì souvisí za pøedpokladu splnìní
podmínky $\chi$ na analyzované matici dat.
\paragraph{}
\label{categorization}
Metoda GUHA pracuje pouze s kategoriálními daty,
protože vzhledem ke zpùsobu generování hypotéz je nutné, aby byl prostor
hypotéz koneèný. Data, která nejsou diskrétní, musí být diskretizovány
pøedem (ve fázi pøípravy dat). Obvykle tak, že se vytvoøí nìkolik 
nepøekrývajících se intervalù, které pokryjí všechny hodnoty v databázi.
\paragraph{}
Typická efektivní realizace GUHA procedury používá "vertikální" reprezentaci
databáze, kde každá hodnota každého atributu je reprezentována øetìzcem bitù
(viz. kapitola \ref{bitstrings}). Každý bit v øetìzci popisuje jeden objekt.
Bit 1 reprezentuje "objekt má tuto hodnotu", zatímco bit 0 znamená
"objekt nemá tuto hodnotu".

\section{Dùležité pojmy metody GUHA}
Zde bude následovat struèný pøehled principù dataminingu dle metody GUHA,
které jsou nezbytné k pochopení popisu samotné problematiky a implementace
zadání dané diplomové práce. Tyto principy jsou obecné a byly ve vìtší èi
menší míøe implementovány v dataminingových systémech pracujících na principu
GUHA - viz \ref{implementations}. Pro zevrubnìjší popis autor doporuèuje
prostudování zejména zdrojù \cite{guha} a \cite{kuchar}.
\subsection{Princip zadání úlohy}
Jak je zmínìno v \ref{categorization}, GUHA pracuje s kategoriálními daty.
Smyslem zadání úlohy je zejména urèení zpùsobu kategorizace tak, aby byl
co nejvhodnìjší a nejrelevantnìjší jak vzhledem k požadované úloze ke zpracování,
tak i ke zkoumaným datùm. Pro tento úèel byly nadefinovány následující pojmy
a prostøedky.
\subsubsection{Datový zdroj}
GUHA pracuje s daty z datových matic. Ty jsou ziskávány pøevážnì jako databázové
tabulky èi výsledky operací nad nimi z databází. Pro tento úèel je nutné 
pøi zadávaní úlohy definovat datový zdroj (momentálnì nejèastìji ve formì
pøípojení k databázi pøes rozhraní ODBC) a alespoò jednu datovou matici.
V relaèním rozšíøení dataminigových procedur GUHA se pracuje nad více tabulkami,
které jsou mezi sebou ve vztahu 1:N. Pro pohodlí se o nich zmiòujeme jako
o hlavní (master) a vedlejších (detail) tabulkách.
\paragraph{}
Dále se z datových matic vybírají datové sloupce, ze kterých se budou vytváøet
atributy obsahující kategorie pro potøebné rozdìlení vstupních dat. Data mohou
patøit do rùzných datových typù. Tyto dále ovlivní možnosti kategorizace.
Stojí za zmínku, že zatím nejkomplexnìjší implementace práce s jednotlivými
sémantickými typy byla realizována v práci \cite{kuchar} - "Experimentální
GUHA procedury".
\subsubsection{Datová sémantika}
Z pohledu datové sémantiky se mohou vyskytovat tyto ètyøi druhy dat:

\label{datasemantics}
\begin{description}
\item[Nominální data] Hodnoty nejsou vzájemnì porovnatelné, napøíklad,
pohlaví nebo oblíbená pøíchu zmrzliny.
\item[Ordinální data] Hodnoty jsou vzájemnì porovnatelné, napøíklad, výše
dosaženého vzdìlání.
\item[Cyklická ordinální data] U tohoto sémantického typu se vychází ze
zkuše\-nosti, že pøi zadá\-vaní koeficientu typu \emph{cyklické intervaly}
(viz \ref{coefficients}) si èasto nevysta\-èíme pouze s ordinál\-ními daty.
Potøebujeme další vlastnost a to, aby první kategorie pøirozenì ná\-sledo\-vala
po poslední. Jako pøíklad mohou po\-sloužit dny v týdnu nebo mìsíce v roce.
\item[Kardinální data] Tato data jsou reprezentována èíselnými typy,
napøíklad, reálnými èísly. Jako pøíklad lze zvolit výši pøíjmu,
vzdalenost od místa bydlištì, výši krevního tlaku a podobnì.
\end{description}


\subsubsection{Kategorie}
Jak již bylo øeèeno výše, metoda GUHA pracuje s diskrétními daty. Data, která
tuto podmínku nesplòují, musí být diskretizována. Diskretizovaný sloupec
se oznaèuje jako atribut a jeho hodnoty jsou kategorie..
\paragraph{}
Pojem kategorie se postupnì vyvíjel v implementacích systému GUHA. Kategorie
mùže být dvojího typu: buï intervalová - obsahuje koneèný poèet intervalù,
který pokrývá zkoumanou množinu dat, anebo výètová, kde výèet opìt pokrývá
zkoumanou množinu dat. V systémech Lisp-Miner a Rel-Miner kategorie
mùže obsahovat buï jenom intervaly anebo jenom výèet. Až v rámci diplomové
práce \cite{kuchar} pojem kategorie byl pøi\-rozenì zobecnìn a imple\-mentován
do systému Ferda DataMiner tak, aby mohl obsahovat jak intervaly, tak i výèet.
Kategorie tedy obsahuje hodnoty, které jsou pokryty sjednocením výètu
a intervalù v kategorii. Podmínkou pro možnost vytváøení intervalù v kategorii
je práce nad daty, které mají sémantický typ ordinální, cyklický ordinální
nebo kardinální (viz \ref{datasemantics}).
\paragraph{}
V rámci diplomové práce \cite{kuchar} byly vytvoøeny prostøedky pro práci
s atributy a kategorii, avšak pro uživatelskou práci byl z dùvodu pracnosti
implementován pouze atribut typu "Each value one category", který, jak plyne
z názvu, umí vytvoøit ze zkoumaného datového sloupce atribut obsahující
výètové kategorie pro každou hodnotu ve sloupci.
\paragraph{}
Atributy "Ekvidistanèní intervaly" a "Ekvifrekvenèní intervaly" ve formì
krabièek pro uživatelskou práci v systému Ferda DataMiner byly implemen\-továny
autorem této diplomové práce, jedná se o pøínos autora. Taktéž byla autorem
implen\-tována krabièka "Statický atribut", která umožòuje pøímou editaci
atributu, vèetnì editace výètu a intervalù u jednotlivých kategorii.

\subsection{Definování množiny relevantních otázek}
Principem metody GUHA je možnost jednoduchého zadání množiny relevantních
otázek. Tyto otázky jsou poté generovány tzv. minerem a postupnì
verifikovány na základì kvantifikátorù, které jsou souèástí zadání úlohy GUHA.
Lze zadat generování boolských a kategoriálních atributù. Zde se podrobnìji
zamìøíme na ty boolské, protože autorem implementována relaèní rozšíøení
4ft a SD4ft minerù vystupují jako jistý druh právì boolských atributù.
Pro zevrubnìjší pochopení tìchto pojmù GUHA autor doporuèuje k prostudování
buï pøímo systém Ferda DataMiner, který se ostatnì od zaèátku vyvíjel pøímo
pro využití k výuce data\-miningu anebo z použité literatury 
\cite{guhabasic}.
\subsection{Boolské atributy}
Následuje definice boolského atributu, jak je uvedena v \cite{guha}.

\newtheorem{booleanattributedef}{Definice booleovského atributu}
\begin{booleanattributedef}
Booleovský atribut je formule splòující následující podmínky.
\paragraph{}
Pokud \emph{a} je atribut a $\alpha$ je neprázdná podmnožina množiny
\{$a_{1},\ldots,a_{k}$\}, pak $A(\alpha)$ je \emph{základní booleovský atribut}.
$\alpha$ je koeficient (podrobnìji v \ref{coefficients}) základního booleovského
atributu $A(\alpha)$. Každý základní booleovský atribut je booleovský atribut.
Pokud $\phi$ a $\psi$ jsou booleovské atributy, pak $\phi \wedge \psi$,
$\phi \vee \psi$ a $\neg\phi$ jsou odvozené booleovské atributy. Každý
\emph{odvozený booleovský atribut} je booleovský atribut.
\end{booleanattributedef}
Dále GUHA pracuje s pojmem \emph{dílèí booleovský atribut}, kterým oznaèuje
booleovské atributy, které vystupují jako operandy v konjunkcích a disjunkcích
booleovských atributù.
\paragraph{}
Základní booleovský atribut $A(\alpha)$ nabývá hodnoty \emph{true} na øádku
\emph{o} matice \emph{M}, pokud $\emph{a}\in\alpha$, kde \emph{a} je hodnota
atributu \emph{a} na øádku \emph{o} - dle odstavce 2 v \cite{guha}.
Hodnota odvozených booleovských atributù se definuje obvyklým zpùsobem
dle následující tabulky:
\paragraph{Hodnoty odvozených boolských atributù}
\paragraph{}
\begin{tabular}{|c|c|c|c|c|}
\hline
$\phi$ & $\psi$ & $\phi \wedge \psi$ & $\phi \vee \psi$ & $\neg\phi$ \\
\hline
true & true & true & true & false \\
\hline
true & false & false & true & false \\
\hline
false & true & false & true & true \\
\hline
false & false & false & false & true \\
\hline
\end{tabular}
\paragraph{}
Hodnota booleovského atributu $\phi$ v øádku o matice \emph{M} se znaèí
$\phi(o,\emph{M})$. Platí, že $\phi(o,\emph{M})=true$, pokud $\phi$ je pravdivý
na øádku \emph{o} matice \emph{M}, jinak $\phi(o,\emph{M})=false$.
\subsection{Zadání množiny relevantních otázek}
Zde popíšeme možnosti nastavení dùležitosti jednotlivých složek zadání
booleovského atributu, jak byly prvnì popsány T.Karbanem v \cite{relminer}
a implementovány do prostøedí Ferda DataMiner T.Kuchaøem v \cite{kuchar}.
\begin{description}
\item[Forced]Booleovský atribut musí být pøítomen v každé relevantní otázce.
Aby bylo dosaženo této podmínky, musí zøejmì každý booleovský atribut
odvozený od jiného \emph{Forced} booleovského atributu (negací, konjunkcí, apod.)
být rovnìž typu \emph{Forced}. Této vlastnosti lze elegantnì využít pøi dìlení
úlohy na disjunktní èásti, které lze øešit oddìlenì a tedy výhodnì distribuovat.
\item[Basic]Má stejný význam jako termín \emph{Basic} pro literál v LISp Mineru.
\emph{Basic} booleovský atribut mùže být používán volnì, tj. mùže, ale nemusí být
použit v konjunkci (resp. disjunkci).
\item[Auxiliary]Má odlišný význam než termín \emph{Remaining} pro literál
v LISp Mineru. \emph{Auxiliary} booleovský atribut smí být použit pouze 
v pøípadì, pokud již byl v konjunkci (resp. disjunkci) použit nìjaký
\emph{Forced} nebo \emph{Basic} booleovský atribut. To znamená, že není možné
vytváøet konjunkce (resp. disjunkce) pouze z \emph{auxiliary} dílèích
booleovských atributù. Booleovské atributy vystupující jako jediné operandy
v rámci jiných booleovských atributù (napø. negace, konjunkce, $\ldots$)
proto nesmí být typu \emph{auxiliary}.
\end{description}
Pro podrobnìjší popis všech možností zadání boolských atributù autor
doporuèuje prostudovat \cite{kuchar}.
\subsection{Koeficienty}
\label{coefficients}
Koeficient je vlastní neprázdná podmnožina všech kategorií základního atributu,
které mohou být použity pøi zpracování úlohy. V prostøedí \mbox{Ferda DataMiner}
použiváme koeficienty následujících typù (pro zjednodušení zápisu místo 
$A(\{1\})$ píšeme zkrácenì pouze $A(1)$ a místo $A(\{1,2\})$ píšeme
pouze $A(1,2)$ apod.).
\begin{description}
\item[Podmnožiny]
Uživatel definuje její minimální a maximální délku. Mìjme napøíklad atribut
\emph{a} s kategoriemi $\{1, 2, 3, 4\}$, pokud chceme vy\-generovat podmnožiny
velikosti 1-2, výstupem budou následující základní booleov\-ské atri\-buty
(v tomto poøadí): $A(1)$, $A(1,2)$, $A(1,3)$, $A(1,4$), $A(2)$, $A(2,3)$,
$A(2,4)$, $A(3)$, $A(3,4$) a $A(4)$.
\item[Intervaly]
Uživatel definuje jejich minimální a maximální délku. Požadavkem je,
aby kategorie byly sousední. Pro atribut \emph{a} s kategoriemi
$\{1, 2, 3, 4, 5\}$ budou intervaly délek 2–3 v základních booleovs\-kých
atri\-butech vypadat takto: $A(1,2)$, $A(1,2,3)$, $A(2,3)$, $A(2,3,4)$,
$A(3,4)$, $A(3,4,5)$ a $A(4,5)$.
\item[Cyklické intervaly]
Podobají se bìžným intervalùm, ale kategorie jsou cyklické. V našem pøíkladì
budou cyklické intervaly délky 3 dávat základní booleovské atributy:
$A(1,2,3)$, $A(2,3,4)$, $A(3,4,5)$, $A(4,5,1)$ a $A(5,1,2)$. Tento typ 
má význam u typu ordinálních dat, kde první kategorie pøirozenì následuje
po poslední. Pøíkladem mohou být dny v týdnu nebo mìsíce v roce.
\item[Levé øezy]
Jde o intervaly s levou hranicí pevnì fixovanou na první kategorii. V našem
pøíkladì levé øezy délky 3 budou základní booleovské atributy:
$A(1)$, $A(1,2)$ a $A(1,2,3)$.
\item[Pravé øezy]
Analogicky k levým øezùm lze definovat pravé øezy. Na stejném pøíkladu pùjde
o základní booleovské atributy: $A(5)$, $A(5,4)$ a $A(5,4,3)$.
\item[Øezy]
Øezy jsou sjednocením levých a pravých øezù, tedy v našem pøíkladu jde 
o základní booleovské atributy: $A(1)$, $A(1,2)$, $A(1,2,3)$, $A(3,4,5)$,
$A(4,5)$ a $A(5)$.
\item[Pevná podmnožina]
Jde o speciální pøípad koeficientu, kde uživatel vybere kategorie, které
budou tvoøit konkrétní základní booleovský atribut. V našem pøíkladu,
vybere-li uživatel kategorie $2$, $3$ a $5$, bude jediným základním 
booleovským atributem na výstupu $A(2,3,5)$. Tento typ koeficientu se objevuje
již v systému Rel Miner a zobecòuje koeficient typu Jedna dílèí kategorie
známý v systému LISp-Miner. Je rovnìž implementován v prostøedí 
\mbox{Ferda DataMiner} T.Kuchaøem v \cite{kuchar}.
\end{description}
Délka pevnì zvoleného koeficientu je dána poètem použitých kategorií.

\subsection{Kategoriální atributy}
S kategoriálními atributy pracují nìkteré dataminigové procedury. Více o použití
kategoriálních atributù se lze doèíst v \ref{miners}.
\subsection{Generování boolských atributù}

\subsection{Princip bitových øetízkù}
\label{bitstrings}

\subsection{Dataminingové procedury}
\label{miners}
\subsection{Existující implementace}
\label{implementations}
\subsubsection{Lisp-Miner}
\subsubsection{Rel-Miner}
\subsubsection{Ferda DataMiner}

\section{Relaèní asociaèní pravidla a virtuální atributy}
\subsection{Úvod}
Zde bude krátce popsáno rozšíøení asociaèních pravidel pro práci s vícero datovými
tabulkami takové, aby byly zachovány všechny vlastnosti metody GUHA a zároveò
aby se umožnil popis zajímavých vzorcù nalazených na více datových tabulkách.
Vlastnosti metody GUHA, zejména zadávání úlohy pro datamining a implementace
pomocí práce s bitovými øetízky zùstanou zachovány. Pro podrobnìjší popis
teorie rozšíøení metody GUHA o pravidla pro relaèní datamining doporuèujeme
prostudovat \cite{relminer}. Protože cílem dané diplomové práce je konkrétní
implementace postupu relaèního dataminingu, zde se zamìøíme pouze na nìkteré
nejdùležitìjší aspekty kritické pro pochopení implementace v rámci prostøedí
Ferda.
\subsection{Virtuální atributy}
Asociáèní pravidla se rozšíøí o možnost práce s více datovými tabulkami.
Datová matice, která byla zkoumána pomocí pùvodních asociaèních pravidel, 
bude hlavní. Další datové tabulky budou rozšíøením hlavní tabulky o nìjaký atribut
a mohou být obecnì ve vztahu 1:N ke hlavní datové matici. Jako pøíklad mùže
posloužit databáze klientù banky, kde v hlavní tabulce máme uloženy osobní
údaje klientù a napøíklad v tabulce transakce jsou uloženy transakce pro úèty
klientù. Dalším pøíkladem je databáze pacientù, kde v rozšíøující tabulce jsou
uložená mìøení krevního tlaku pro jednotlivé pacienty.
\paragraph{}
Základním pojmem, o který se rozšíøí pùvodní asociaèní pravidla, je
\emph{virtuální atribut}. Virtuální atributy jsou novì pøidané sloupce
k analyzované datové matici. Tyto sloupce se do pùvodné datové matice zpìtnì
neukládají, avšak dolování dat probíhá, jakoby tyto novì vytvoøené sloupce
v pùvodní matici byly - vytváøejí se (nebo v pøípadì konkrétní implementace
v rámci dané diplomové práce se vytváøí pouze jejich reprezentace za pomocí
bitových øetízkù) za bìhu.
\paragraph{}
Pro názornost uveïme nìkolik pøíkladù virtuálních atributù. Použijeme
databázi klientù fiktivní banky Barbora, kde hlavní tabulka obsahuje
základní data o klientech a tabulka transakcí obsahuje pohyb na jejich úètech.
Zkoumejme následující hypotézu:
\begin{eqnarray}
\label{eq:aggrattributeexample}
TYPE="PRIJEM" \& AMOUNT > AVG(AMOUNT)*2 \Rightarrow_{80\%} \nonumber\\
OPERATION="PREVOD NA UCET"
\end{eqnarray}
Zde jsme použili jeden virtuální atribut $AVG(AMOUNT)*2$, kde $AVG$ je
agregaèní funkce SQL. Hypotéza mùže znamenat zjednodušenì øeèeno,
pøedpoklad o faktu, že vyšší pøísuny penìz na úèet probíhají spíše
bezhotovostnì. Tato hypotéza mùže a nemusí mít smysl - autor neaspiruje
na odborníka v bankovnictví.
Uvažme další hypotézu:
\begin{eqnarray}
\label{eq:hyprattributeexample}
TYPE="PRIJEM" \& AMOUNT > AVG(AMOUNT)*2 \Rightarrow_{80\%} \nonumber\\
OPERATION="PREVOD NA UCET"
\end{eqnarray}
\paragraph{}
Z výše uvedeného lze vidìt, jak rozdìlujeme virtuální atributy. Uvažujeme
dva základní druhy, jak jsou chápany
v \cite{relminer}. Oba druhy atributù jsou v rámci dané práce implementovány
do prostøedí Ferda.
\paragraph{}
První skupinou jsou sloupce vzniklé jako výsledek pøedzpracování dat z databáze.
Jmenují se agregaèní atributy. Druhou skupinou jsou virtuální atributy vzniklé
jako výsledek bìhu nìkteré z dataminingových úloh - konkrétnì napøíklad mùžeme
použít míru platnosti hypotéz pro jednotlivé záznamy v hlavní tabulce jako
nový datový sloupec.
\subsubsection{Agregaèní atributy}
Agregaèní atribut se reprezentuje novým virtuálním sloupcem (který se
kvùli samotné podstatì principu vituálního atributu neukládá do databáze),
Ten vzniká jako výsledek funkce nad daty z více datových tabulek, pøièemž
zmínìná funkce pøiøadí každému objektu v hlavní datové matici reálné èíslo
vypoèítané na základì hodnot v odpovídajících sloupcích z vedlejších datových 
matic.
\paragraph{}
Jako pøíklady takto uvažovaných funkcí mùže posloužit mimo jiné
množina SQL agregaèních funkcí, která vyhovuje výše uvedené definici. Dále mùžeme
uvažovat množinu obecnìjších funkci zpracovávajících data z vedlejších
datových tabulek. Pøíklad implementace takových funkcí mùžeme naleznout v
\cite{duchacek} - jedná se o implementaci ve formì skriptu napsaného
v jazyce C\# interpretovaného
pøímo za bìhu. Takto lze implementovat i obecnìjší agregaèní atributy než
pouze za použití SQL agregaèních funkcí.

\subsubsection{Hypotézové atributy}
Narozdíl od agregaèních atributù, hypotézové atributy nelze získat za pomocí
agregaèních funkcí nad daty z hhlavní a vedlejších datových tabulek. Tyto 
atributy vzníkají jako výsledek bìhu dataminingových úloh nad vedlejšími
datovými tabulkami a dále se používají pøi bìhu dataminingových úloh nad
hlavní datovou tabulkou. Ústøední myšlenkou je zavést definici atributu
se semantikou hypotézy (ve smyslu metody GUHA), jehož hodnotou by byla konkrétnì
síla nebo platnost hypotézy nad vedlejšími datovými tabulkami a takto
nadefinovaný sloupec by se použil "o úroveò výše" - pro generaci hypotéz
pøi bìhu další dataminingové úlohy.


