TODO Categorial Cedent : minlen, maxlen, attributes[]

BitString(BitStringProvider* bsp, ? bitStringId) implements Entita;

enum TypeOfCoeficientEnum
{
	Subset,
	Intervals,
	Cuts,
	...
}

Atom(BitStringProvider* bsp, ? attributeId, TypeOfCoeficientEnum toce, int minLen, int MaxLen) implements Entita
AtomFixedSubset(BitStringProvider* bsp, ? attributeId, string[] fixedSubsetMembers) implements Entita


